<?php

class MainController extends CoreController 
{
    // On a tout factorisé dans le CoreController !
    // Du coup, il ne reste plus rien dans le MainController
    // On le garde au cas ou, mais si une fois le projet fini il et toujours vide,
    // on s'en débarassera !
}